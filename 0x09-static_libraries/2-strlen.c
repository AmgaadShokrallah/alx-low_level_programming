#include "main.h"
#include <stdio.h>

/**
 *  _strlen - unction that returns the length of a string.
 *
 * @s: parameter
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int L;

	for (L = 0; s[L] != '\0'; L++)
	{
	}
	return (L);
}
