#include "main.h"
#include <stdio.h>

/**
 * _atoi - function that convert a string to an integer.
 *
 * Return: Always 0.
 */

int _atoi(char *s)
{
	int unsign = 0;
	int sign = 1;

	if (s == '-')
		sign* = -1;
	else if (*s >= '0' && *s < '10')


}
