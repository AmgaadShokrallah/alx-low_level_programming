#include <stdio.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 *@n: the integer type input  in function.
 *
 * Return: Always 0 (Success)
*/

int _abs(int)
{
	if (n > 0)
		return (n);
	else
		return (-n);


}
